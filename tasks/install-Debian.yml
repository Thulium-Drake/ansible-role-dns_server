---
- name: 'Install PowerDNS'
  apt:
    name:
      - 'pdns-server'
      - "pdns-backend-{{ dns_server_backend }}"
      - 'python-dnspython'
      - 'dnsutils
    state: 'present'
    update_cache: true
